sudo: required
dist: trusty

language: cpp
compiler:
  - g++
  
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-6
    - g++-6
    - txt2man

before_script:
  - cd ./sources
  - ls
  - g++ --version
  - cmake .
script:
  - make
  - ./output/vcdMaker
